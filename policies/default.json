{
  "default_action": "require_approval",
  "global_deny_patterns": [
    ".env",
    ".ssh",
    "id_rsa",
    "private key",
    "secret_key",
    "api_key",
    "access_token",
    "rm -rf",
    "curl ",
    "wget ",
    "| sh",
    "base64 -d"
  ],
  "tools": {
    "demo.weather": {
      "action": "allow",
      "reason": "Weather lookups are read-only and low risk."
    },
    "demo.read_file": {
      "action": "allow",
      "reason": "Demo file reads are allowed unless the path matches a denied pattern.",
      "deny_patterns": [
        ".env",
        ".ssh",
        "id_rsa",
        "secret"
      ]
    },
    "demo.write_file": {
      "action": "require_approval",
      "reason": "File writes mutate state and should be reviewed first.",
      "risk_level": "high"
    },
    "demo.shell_run": {
      "action": "block",
      "reason": "Shell execution is disabled in the starter policy.",
      "risk_level": "critical"
    }
  }
}

